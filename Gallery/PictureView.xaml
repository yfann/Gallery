<Window x:Class="Gallery.PictureView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding PicInfo}" Background="Black" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="viewer" 
               Grid.Row="0"
               Grid.RowSpan="2"
               Source="{Binding Picture}"
               Stretch="Uniform"
               VerticalAlignment="Center"
               HorizontalAlignment="Center" 
               MouseEnter="viewer_MouseEnter" 
               MouseLeave="viewer_MouseLeave"
               MouseDown="viewer_MouseDown">
        </Image>
        <StackPanel x:Name="tools" 
                    Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Visibility="Collapsed">

        </StackPanel>

    </Grid>
</Window>
